{node_name, "{{name}}"}.

{app_name, "{{name}}"}.

{log_dir, "/var/log/{{name}}"}.

{install_dir, "/opt/{{name}}"}.

{tmp_dir, "/var/tmp/{{name}}"}.

{lib_dir, "/var/lib/{{name}}"}.

{erlang_cookie, "{{name}}cookie"}.

{erlang_max_ports, 4096}.

{erlang_max_processes, 262144}.

{lager_log_level, info}.

{proto_dist, xepmd_inet}.